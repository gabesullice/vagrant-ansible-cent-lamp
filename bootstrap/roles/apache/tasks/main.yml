---

- name: ensure apache2 is present
  apt: name={{ item }} state=present
  with_items:
    - apache2
    # - httpd-devel
    # - httpd-tools
  sudo: yes
  tags:
    - apache

- name: ensure apache is running
  service: name=apache2 state=running enabled=yes
  sudo: yes
  tags:
    - apache

#- name: configure docroot
#  replace: dest=/etc/apache2/apache2.conf regexp='^DocumentRoot.*' replace='DocumentRoot /vagrant_data'
#  sudo: yes
#  notify:
#    - reload apache
#  tags:
#    - apache

#- name: ensure virtual hosts config is present
#  copy: src=nvh.conf dest=/etc/httpd/conf.d/nvh.conf mode=0644
#  sudo: yes
#  notify:
#    - reload httpd
#  tags:
#    - apache
